server {
    listen 80;
    # server_name localhost
 
    root  /var/www/html;
    index index.html;
 
    # access_log /var/log/nginx/ssl-access.log;
    # error_log  /var/log/nginx/ssl-error.log;
 
    # location / {
    #     try_files $uri $uri/;
    # }
 
}